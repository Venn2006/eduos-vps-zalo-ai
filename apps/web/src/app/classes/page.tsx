import React from 'react';
import { PageShell } from '@/components/layout/PageShell';

export default function ClassesPage() {
  return (
    <PageShell 
      title="Lớp học" 
      description="Quản lý danh sách lớp học và giáo viên"
      primaryAction="Thêm lớp học"
    />
  );
}
